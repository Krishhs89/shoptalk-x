# ShopTalk-X — full environment (all components).
#
# Per-component files live in requirements/ (data.txt, embeddings.txt,
# eval.txt, serving.txt, ui.txt, monitoring.txt, voice.txt, dev.txt) --
# install only what a given piece needs, e.g.:
#   pip install -r requirements/serving.txt   # just the API
#   pip install -r requirements/ui.txt        # just the Streamlit frontend
#
# This file installs everything, for local end-to-end development.
-r requirements/serving.txt
-r requirements/monitoring.txt
-r requirements/eda.txt
-r requirements/ui.txt
-r requirements/voice.txt
-r requirements/dev.txt
