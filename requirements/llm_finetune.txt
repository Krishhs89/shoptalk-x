# LoRA/QLoRA instruction fine-tuning for the generation LLM (Kaggle GPU only
# -- NOT part of the main requirements.txt aggregate, since it needs a real
# GPU + bitsandbytes and isn't used by the serving path).
-r embeddings.txt
peft==0.13.2
bitsandbytes==0.44.1
trl==0.12.1
