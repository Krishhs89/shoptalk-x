# RAG + LLM serving + FastAPI backend (Day 4) -- shoptalk.rag.*, shoptalk.api.*
# (Ollama itself is a separate binary/container, not a pip package -- see README)
-r eval.txt
-r counting.txt
langchain-core==0.3.29
langchain-ollama==0.2.2
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.17
prometheus-client==0.21.1
